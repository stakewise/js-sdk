{
  "name": "js-sdk",
  "version": "0.1.0",
  "description": "StakeWise JS SDK",
  "homepage": ".",
  "private": true,
  "scripts": {
    "prepare": "npm i lerna && lerna bootstrap && lerna run typechain && npm run link",
    "build": "lerna run build",
    "test": "lerna run test",
    "link": "npm i ./packages/stakewise-methods --prefix=./packages/stakewise-widget && npm i ./packages/stakewise-methods --prefix=./packages/stakewise-dev && npm i ./packages/stakewise-widget --prefix=./packages/stakewise-dev",
    "test-coverage": "lerna run test-coverage"
  },
  "license": "AGPL-3.0-only",
  "devDependencies": {
    "@babel/preset-env": "7.16.11",
    "@babel/preset-typescript": "7.16.7",
    "@types/jest": "27.4.0",
    "husky": "7.0.4",
    "jest": "27.5.1",
    "jest-cli": "27.5.1",
    "ts-jest": "27.1.3"
  },
  "dependencies": {
    "lerna": "4.0.0"
  }
}
